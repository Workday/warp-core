scoverage {
    // Excluding autogenerated Tables
    excludedPackages = ["com.workday.warp.persistence.model", "com.workday.warp.examples"]
    excludedFiles = []

    // Default Coverage Type is "Statement"
    minimumRate = 0.89
    scoverageVersion = "${versions.scalacScoverage}"
}